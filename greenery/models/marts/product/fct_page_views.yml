version: 2

models:
  - name: fct_page_views
    description: All page view events from greenery events data.
    columns:
      - name: session_length_minutes
        description: length of a single session.
        tests:
          - positive_values